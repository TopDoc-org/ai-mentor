import { environment } from '../../environments/environment';

// Base URL of the backend (Zenamaze frontend; backend route unchanged, mounted at /mentor in TopDoc-Backend).
export const API_BASE = `${environment.serverUrl}/mentor`;
